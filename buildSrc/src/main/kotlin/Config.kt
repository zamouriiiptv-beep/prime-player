/** Single source of truth for the Android configuration every module shares. */
object Config {
    const val COMPILE_SDK = 34
    const val MIN_SDK = 21      // old TV boxes still run Android 5
    const val TARGET_SDK = 34
    const val JAVA_VERSION = 17
}
